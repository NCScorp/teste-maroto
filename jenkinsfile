node('master') {
    stage('Checkout') {
			
        timeout(time: 3600, unit: 'SECONDS') {
            checkout scm
        }
    }

    stage('script'){
        sh 'cat utils/sas.py'
    }

    stage('Clean') {
        deleteDir()
    }
}